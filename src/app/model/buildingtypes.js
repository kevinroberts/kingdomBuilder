define("buildingtypes", ["jquery" ], function ($) {
    'use strict';

    //  define all the possible building types for a kingdom
    return {
        NONE:   'none',
        HOUSING: 'housing',
        STORAGE: 'storage'
    };

});